
server:
  port: 9762

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:9761/eureka/

spring:
  zipkin:
    base-url: http://localhost:9411
  application:
    name: service-hi

management:
  endpoints:
    web:
      exposure:
        include: "*"
      cors:
        allowed-origins: "*"
        allowed-methods: "*"



project:
  name: fnn